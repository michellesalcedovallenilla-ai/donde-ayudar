# Netlify build for Donde Ayudar — static Astro site.
# Security/caching headers live in public/_headers (copied into dist/ on build).

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
