# Netlify build configuration for Donde Ayudar (static Astro site).
# Security/caching headers live in public/_headers (Netlify reads it automatically).

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Static prerendered site — no redirects/functions needed.
